vcs - version controling sys

git config --global user.name "luk3Sky"
git config --global user.email "ganindudananja@gmail.com"

git init
git add .
git commit -m "Comment"

git rm <file> to delete
git mv <file1> <file2> to rename


git checkout <commit message id chars> -- <filename or names>
// rollback to a specific commit





